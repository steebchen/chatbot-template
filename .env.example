# PLOY_AI_URL and PLOY_AI_TOKEN are injected automatically by Ploy (`ai: true`
# in ploy.yaml), both in deployments and when running `ploy dev`. Nothing to
# set by hand unless you run plain `next dev` (`pnpm dev:next`).
# PLOY_AI_URL=
# PLOY_AI_TOKEN=
